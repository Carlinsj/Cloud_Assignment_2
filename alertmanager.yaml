global:
  resolve_timeout: 5m

route:
  receiver: slack-notifications

receivers:
- name: slack-notifications
  slack_configs:
  - api_url: "<Slack_Webhook_URL>"
    channel: "#assignments"
    send_resolved: true